package com.example.day3demo;

public class MonopolyState {

    public enum Token
    {
        BATTLESHIP, RACECAR, BOOT, THIMBLE, TOP_HAT

    }

    public void moveToken(int playerId, Token piece, int spaces)
    {
        if(piece == Token.RACECAR)
        {

        }
    }
    // for each player
        // Real estate (locations/names)
        // cards (events)
        // money
        // moving pieces
            // location

    // all other things not associated with a player
        // Real estate (locations/names)
        // cards (events)
        // money
        // moving pieces

    // Dice

    // win/lose conditions

}
